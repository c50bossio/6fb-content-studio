# 6FB Content Studio does not require a checked-in environment file for normal use.
#
# If a local development or packaging task requires a revocable token, copy this
# file to `.env` and add only that token there. Never store account passwords,
# customer data, Apple signing certificates, or production secrets in this repo.
# Release credentials belong in GitHub Actions secrets.
