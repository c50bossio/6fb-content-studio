// Instagram Login issues Instagram User access tokens. Those tokens must use
// the Instagram Graph host; the Facebook Graph host only accepts the distinct
// Facebook Login token model. Keep version changes centralized and verify them
// against Meta's official version-support table before release.
export const INSTAGRAM_GRAPH_ORIGIN = 'https://graph.instagram.com/v23.0';
