# v1.5.50 macOS-only release readiness

Last updated: 2026-07-23

## Candidate identity

- Proposed release tag: `v1.5.50`.
- Release source change: Settings connection-workflow redesign merged through
  pull request #50 as
  `16cde66ba23218b5e0b22a106ae4ae49cd9cc90c`.
- Previous public release: [v1.5.49](https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.49).
  `v1.5.47` remains a private historical draft; `v1.5.50` is the next available
  patch version.
- Target platform: macOS arm64 only.
- Source `package.json` remains `1.5.39` by repository convention; the tag
  workflow stamps the official artifact version while packaging.

## Verified pre-tag proof

- [x] The full exact-main suite passed at `16cde66`: 65 unit tests, 82 IPC
      contracts, Python/runtime checks, production build, and isolated Electron
      IPC smoke.
- [x] The responsive visual matrix captured 87 states at 375, 768, and 1440 px
      with zero layout, console, or network findings and three focus contracts.
- [x] Strict workspace validation passed on the same exact main commit.
- [x] The release workflow is macOS-only, stages exactly four Mac artifacts,
      and does not call the manual Windows validator.
- [x] This release body and readiness checklist are tracked before tagging.

## Remaining release gates

- [ ] Merge this release-preparation record, resolve the exact `origin/main`
      tag target, and re-run the exact-main release checks.
- [ ] Confirm `v1.5.50` remains unused immediately before tagging.
- [ ] Create and push annotated tag `v1.5.50` after the exact-main gates pass.
      Pushing the tag creates a private draft only; it cannot make the release
      public.
- [ ] Confirm the tagged macOS workflow passes runtime build, full suite,
      signing, notarization, stapling, exact-four-asset draft staging, and
      staged-DMG smoke.
- [ ] Confirm the private draft is non-prerelease and contains exactly:
  - `6FB-Content-Studio-arm64.dmg`
  - `6FB-Content-Studio-arm64.zip`
  - `6FB-Content-Studio-arm64.zip.blockmap`
  - `latest-mac.yml`
- [ ] Run the separately approval-gated promotion workflow only after the
      staged-draft acceptance gates pass.
- [ ] Verify the public release is non-draft, non-prerelease, Mac-only, and
      has the exact four-asset manifest; record sizes, SHA-256 digests, updater
      metadata, and public-DMG smoke evidence.

## Stop conditions

- Stop if the proposed tag target changes after exact-main verification.
- Stop if a release workflow is already running.
- Stop on any full-suite, signing, notarization, packaging, updater,
  asset-manifest, console, network, or browser-sign-in failure.
- Do not delete, skip, or weaken checks to obtain a release pass.

## Boundary

This checklist prepares a reviewable Mac-only release candidate. It does not
create a tag, promote a draft, publish a GitHub Release, post to social media,
or contact customers.
