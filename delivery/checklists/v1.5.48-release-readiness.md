# v1.5.48 macOS-only release readiness

Last updated: 2026-07-22

## Candidate identity

- Proposed release tag: `v1.5.48`.
- Release source change: Thumbnail Maker, merged through pull request #40 as
  exact `origin/main` commit
  `8e42109eedba1b4eef214f55846a2a2479fc512e`. The release tag target must be
  resolved from current `origin/main` after this tracked preparation record is
  merged; stop if it changes before exact-main verification.
- Previous public release: [v1.5.46](https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.46).
  `v1.5.47` is an existing private draft for Smart Live Trends and predates
  Thumbnail Maker, so it must not be promoted as this release.
- Target platform: macOS arm64 only.
- Source `package.json` remains `1.5.39` by repository convention; the tag
  workflow stamps the official artifact version while packaging.

## Pre-tag gates

- [x] Confirm `v1.5.47` is already reserved by an existing private draft and
      selects the next available patch version, `v1.5.48`.
- [x] Confirm the exact current `origin/main` workflow is macOS-only, stages
      exactly four Mac artifacts, and does not call the manual Windows validator.
- [x] Confirm required Apple signing/notarization and GitHub release secret
      names are validated by the workflow without reading their values.
- [x] Record the v1.5.48 public release body and this readiness checklist.
- [x] Merge the release-preparation and runtime-lock records, resolve exact
      tag target `8e29d4c8ffc20ef5b0e78c1d31b8dce8655c927b`, and confirm it matches
      the verified correction tree.
- [x] Re-run the full suite, production build, documentation contracts, release
      workflow lint, and normal/strict/portable workspace validators on that
      exact proposed tag target.
- [x] Create and push annotated tag `v1.5.48` after the exact-main gates
      pass. Pushing the tag triggers the signed macOS build and creates a
      private certified draft; it cannot make the release public.

## Draft acceptance gates

- [x] Confirm annotated tag `v1.5.48` resolves to the approved commit.
- [x] Complete the macOS workflow: runtime build, full suite, signing,
      packaging, notarization, stapling, updater normalization, and draft
      staging.
- [x] Pass the staged-DMG stapling, strict-signature, Gatekeeper, and version
      smoke before making the draft available for owner testing.
- [ ] Run a creator acceptance check from the certified draft: select a local
      video, create a three-option package, reopen it from Thumbnail Library,
      and export one final cover.

## Public promotion gates

- [ ] Obtain separate explicit owner approval to manually run the public
      promotion workflow for the certified draft.
- [ ] Verify the public release is non-draft, non-prerelease, and contains
      exactly these four non-empty assets:
  - `6FB-Content-Studio-arm64.dmg`
  - `6FB-Content-Studio-arm64.zip`
  - `6FB-Content-Studio-arm64.zip.blockmap`
  - `latest-mac.yml`
- [ ] Verify no Windows executable, Windows blockmap, or `latest.yml` appears.
- [ ] Independently download the public arm64 DMG, match its published digest,
      validate stapling and strict code signature, pass Gatekeeper assessment,
      and launch from a disposable app profile as version `1.5.48`.

## Stop conditions

- Stop if the tagged draft is changed, replaced, or its exact manifest no
  longer matches before promotion.
- Stop if a release workflow is already running.
- Stop on any full-suite, runtime, signing, notarization, packaging, updater,
  asset-count, digest, launch, console, network, or creator-flow failure.
- Do not delete, skip, or weaken checks to obtain a release pass.

## Boundary

This checklist prepares a reviewable Mac-only release candidate. It does not
create a tag, promote a draft, publish a GitHub Release, post to social media,
or contact customers.
