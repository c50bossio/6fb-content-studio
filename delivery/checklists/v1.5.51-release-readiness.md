# v1.5.51 macOS-only release readiness

Last updated: 2026-07-23

## Candidate identity

- Proposed release tag: `v1.5.51`.
- Release source change: Instagram Login trend-source correction merged through
  pull request #53 as exact `origin/main` commit
  `b8556606dcd753aeb8c13da7e3bfc9f318708615`. Its application tree is identical
  to the fully tested implementation commit
  `50669423c50cabe6f9d326d5a760ad230829caad`.
- Previous public release: [v1.5.50](https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.50).
  `v1.5.51` is the next available patch version.
- Target platform: macOS arm64 only.
- Source `package.json` remains `1.5.39` by repository convention; the tag
  workflow stamps the official artifact version while packaging.

## Verified pre-tag proof

- [x] The corrected implementation passed the complete local suite: 66 unit
      tests, IPC and source contracts, CDP checks, documentation contracts,
      Python/runtime checks, production build, and isolated Electron IPC smoke.
- [x] The trend service made a bounded, read-only check with the connected
      account: Google and Content Planner were live; the authorized Instagram
      account returned four recent media items. No credential values were
      logged and no social content was created, scheduled, or changed.
- [x] Regression coverage requires the Instagram Login graph origin, a single
      bounded account-media request, accurate account-signal labelling, and
      retention of account ideas when Google returns a full response.
- [x] The tag workflow is macOS-only, stages exactly four Mac artifacts, and
      does not call the manual Windows validator.
- [x] This release body and readiness checklist are tracked before tagging.

## Required release gates

- [ ] Merge this release-preparation record, resolve the exact proposed tag
      target from current `origin/main`, and stop if it differs from the
      reviewed candidate without a fresh verification pass.
- [ ] Re-run the full suite and exact-main release checks on that target.
- [ ] Confirm `v1.5.51` is unused immediately before tagging.
- [ ] Obtain explicit owner approval to create and push annotated tag
      `v1.5.51`. Pushing the tag creates a private draft only; it cannot make
      the release public.
- [ ] Confirm the tagged macOS workflow passes runtime build, full suite,
      signing, notarization, stapling, exact-four-asset draft staging, and
      staged-DMG smoke.
- [ ] Confirm the private draft is non-prerelease and contains exactly:
  - `6FB-Content-Studio-arm64.dmg`
  - `6FB-Content-Studio-arm64.zip`
  - `6FB-Content-Studio-arm64.zip.blockmap`
  - `latest-mac.yml`
- [ ] Perform an authenticated Mac acceptance check of Trending from the
      staged build: it must show an honest authorized-account Instagram signal
      without a source error.

## Public promotion gates

- [ ] Obtain separate owner approval before running the promotion workflow.
- [ ] Verify the public release is non-draft, non-prerelease, Mac-only, and has
      the exact four-asset manifest; record sizes, SHA-256 digests, updater
      metadata, and public-DMG smoke evidence.

## Stop conditions

- Stop if the proposed tag target changes after exact-main verification.
- Stop if a release workflow is already running.
- Stop on any full-suite, signing, notarization, packaging, updater,
      asset-manifest, console, network, or authenticated-Trending failure.
- Do not delete, skip, or weaken checks to obtain a release pass.

## Boundary

This checklist prepares a reviewable Mac-only release candidate. It does not
create a tag, promote a draft, publish a GitHub Release, post to social media,
or contact customers.
