# v1.5.53 macOS-only release readiness

Last updated: 2026-07-23

## Candidate identity

- Proposed release tag: `v1.5.53`.
- Release source change: Smart Trends Google-fit correction merged through pull
  request #58 as exact `origin/main` commit
  `b1ea0ea9b4ded2f150dcf7abac54c163c43a7b5c`. It merges reviewed implementation
  commit `6dbed050dce5b2d251585b09137a9c5745bab7a8`.
- Previous public release: [v1.5.52](https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.52).
  `v1.5.53` is the next available patch version.
- Target platform: macOS arm64 only.
- Source `package.json` remains `1.5.39` by repository convention; the tag
  workflow stamps the official artifact version while packaging.

## Verified pre-tag proof

- [x] Exact merged `main` commit `b1ea0ea9b4ded2f150dcf7abac54c163c43a7b5c`
      passed typecheck, 67 unit tests, 82 IPC contract checks, CDP checks,
      documentation contracts, Python checks, production build, and isolated
      Electron IPC smoke in a fresh worktree.
- [x] A fresh macOS-arm64 bundled runtime build passed `--runtime-check`;
      `cv2`, `mediapipe`, `mlx_whisper`, `scipy.interpolate`, and `yaml` all
      reported healthy.
- [x] Regression coverage excludes unrelated all-low-fit Google headlines,
      preserves direct barber-domain headlines such as `Barber Battle 2026`,
      and does not weaken the fixed 20-point useful-fit threshold.
- [x] The tag workflow is macOS-only, stages exactly four Mac artifacts, and
      does not call the manual Windows validator.
- [x] This release body and readiness checklist are tracked before tagging.

## Required release gates

- [ ] Merge this release-preparation record, resolve the exact proposed tag
      target from current `origin/main`, and stop if it differs from the
      reviewed candidate without a fresh verification pass.
- [ ] Re-run the full suite and exact-main release checks on that target.
- [ ] Confirm `v1.5.53` is unused immediately before tagging.
- [ ] Obtain explicit owner approval to create and push annotated tag
      `v1.5.53`. Pushing the tag creates a private draft only; it cannot make
      the release public.
- [ ] Confirm the tagged macOS workflow passes runtime build, full suite,
      signing, notarization, stapling, exact-four-asset draft staging, and
      staged-DMG smoke.
- [ ] Confirm the private draft is non-prerelease and contains exactly:
  - `6FB-Content-Studio-arm64.dmg`
  - `6FB-Content-Studio-arm64.zip`
  - `6FB-Content-Studio-arm64.zip.blockmap`
  - `latest-mac.yml`

## Public promotion gates

- [ ] Obtain separate owner approval before running the promotion workflow.
- [ ] Verify the public release is non-draft, non-prerelease, Mac-only, and has
      the exact four-asset manifest; record sizes, SHA-256 digests, updater
      metadata, and public-DMG smoke evidence.

## Stop conditions

- Stop if the proposed tag target changes after exact-main verification.
- Stop if a release workflow is already running.
- Stop on any full-suite, signing, notarization, packaging, updater, asset-
      manifest, console, network, or authenticated-flow failure.
- Do not delete, skip, or weaken checks to obtain a release pass.

## Boundary

This checklist prepares a reviewable Mac-only release candidate. It does not
create a tag, promote a draft, publish a GitHub Release, post to social media,
or contact customers.
