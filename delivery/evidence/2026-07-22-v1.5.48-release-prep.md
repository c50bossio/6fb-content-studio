# v1.5.48 macOS release preparation evidence

Date: 2026-07-22

## Scope

This is preparation evidence only. It records the release route for Thumbnail
Maker after it merged to `origin/main` through pull request #40 as
`8e42109eedba1b4eef214f55846a2a2479fc512e`. It does not create a tag, package
a signed installer, publish a GitHub Release, or prove a certified draft.

`v1.5.46` is the current public macOS-only release. `v1.5.47` already exists as
a private Smart Live Trends draft whose tag resolves to
`0e5600d7855262e3d42cc671ac33f427822506ad`, before Thumbnail Maker merged. The
next unused semantic patch tag is therefore `v1.5.48`.

## Release-path checks

- Live `origin/main` resolves to
  `8e42109eedba1b4eef214f55846a2a2479fc512e` before this release-preparation
  record is merged.
- The tag-triggered workflow is macOS-arm64-only. It runs the full suite,
  validates the bundled runtime, signs, notarizes, staples, and stages exactly
  these four private-draft artifacts:
  `6FB-Content-Studio-arm64.dmg`, `6FB-Content-Studio-arm64.zip`,
  `6FB-Content-Studio-arm64.zip.blockmap`, and `latest-mac.yml`.
- The manual publication workflow requires an existing non-prerelease draft,
  rechecks the exact four-asset manifest, then runs anonymous public-asset and
  DMG smoke checks after promotion.
- Windows validation is manual and non-publishing; it cannot run from the
  production tag workflow.
- Local integration proof recorded with pull request #40 includes TypeScript,
  62 unit tests, 80 IPC contracts, documentation contracts, production build,
  isolated Electron IPC persistence smoke, and an 81-state responsive visual
  audit with zero reported findings. This local proof is not a substitute for
  exact-main pre-tag verification or the certified workflow result.

## Remaining gates

1. Merge this preparation record, resolve the exact current `origin/main` tag
   target, and rerun the candidate preflight on that target.
2. Create the annotated `v1.5.48` tag to stage a private signed, notarized Mac
   draft.
3. Review the certified draft and complete the creator Thumbnail Maker
   acceptance check.
4. Obtain a separate owner decision before public promotion.
