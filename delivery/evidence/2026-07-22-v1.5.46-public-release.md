# v1.5.46 public release certification

Date: 2026-07-22

## Identity

- Release: https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.46
- Annotated tag: `v1.5.46`
- Exact target: `f81b63b7b9400fb76cd37d399f366b9a3fbb2aed`
- Workflow run: https://github.com/c50bossio/6fb-content-studio/actions/runs/29950002444
- Published: 2026-07-22 19:29:54 UTC
- Platform: macOS arm64 only

The tag peels to the exact approved tag-target commit. The release is
non-draft and non-prerelease.

## Workflow proof

All six jobs completed successfully:

1. `validate-tag`
2. `release-mac`
3. `stage-release`
4. `smoke-staged-mac`
5. `publish-release`
6. `smoke-published-mac`

The workflow rebuilt the bundled runtime, ran the complete suite, signed and
notarized the app and DMG, staged the exact manifest in a draft, certified the
downloaded staged DMG, published only after that smoke passed, then ran the
anonymous public-manifest and public-DMG checks.

## Public asset manifest

| Asset | Bytes | SHA-256 |
|---|---:|---|
| `6FB-Content-Studio-arm64.dmg` | 417493620 | `d1f1b1aad04ba25a70a945b7286f67e4d7da718d100b6cf47fcf860f2b48bde8` |
| `6FB-Content-Studio-arm64.zip` | 405137097 | `7edf2b2b83c63f59f8920e82d766bcd21a4ce4b8f1e324ddb199a9ab2e4e0969` |
| `6FB-Content-Studio-arm64.zip.blockmap` | 421084 | `7373cfd9b0df7da0ceffa76ea52140899c4de1e0d4ad2ea64a8d4786918938a3` |
| `latest-mac.yml` | 354 | `775377e1bdf3b166d0f39828304c21ee31080226fabf3c8b04273edb26e6215c` |

The anonymous manifest verifier accepted exactly these four non-empty files.
No Windows executable, Windows blockmap, or general `latest.yml` is present.

## Independent public-download proof

The public arm64 DMG was downloaded anonymously from the release URL into a
disposable local directory. Its SHA-256 matched the release API digest. The
downloaded DMG then passed:

- notarization stapling validation;
- deep strict code-signature verification;
- Gatekeeper assessment as Notarized Developer ID software;
- bundle version `1.5.46`;
- mounted app discovery; and
- a real launch with a disposable user-data profile and a live renderer titled
  `6FB Content Studio`.

For the separate launch proof, the mounted public app executable was started
with `SIXFB_USER_DATA_DIR` pointing to a new disposable directory and a local
remote-debugging port. The process stayed alive, created Chromium profile state,
and exposed a page target titled `6FB Content Studio` whose URL resolved inside
the mounted app's `app.asar/out/renderer/index.html`. It was then terminated and
the DMG detached. This launch was independent of the workflow smoke, which
certifies stapling, signature, Gatekeeper, and version but does not launch.

`latest-mac.yml` was also downloaded anonymously and matched its published
SHA-256. It reports version `1.5.46`, size `405137097`, and references only
`6FB-Content-Studio-arm64.zip`, with no DMG or Windows updater path.

## Boundary

This evidence certifies the public macOS arm64 release and updater metadata. It
does not claim Windows publication, Windows signing, social posting, scheduler
execution, customer messaging, or payment activity.
