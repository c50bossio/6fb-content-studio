# 6FB Content Studio v1.5.43

Status: published 2026-07-21.

## What changed

- Improved macOS clip-processing reliability by ensuring the packaged MLX
  runtime includes the Metal shader library required for local transcription
  and pipeline features.
- Added a packaging assertion so incomplete macOS pipeline runtimes fail the
  build instead of reaching users.
- Refreshed supported dependency locks and cleared the current npm security
  audit.

## Verification

- Production Electron build passed.
- Unsigned app launch and dashboard rendering passed.
- The packaged pipeline completed a disposable offline clip extraction and
  produced an inspected 1080x1920 H.264/AAC clip.
- A local Developer ID-signed candidate passed strict signature, hardened
  runtime, embedded dependency, and launch checks.
- The exact merged commit passed the clean npm audit, production build,
  packaged-runtime, pipeline, FFmpeg, FFprobe, and MLX Metal checks.
- The macOS and Windows tag workflows completed successfully and published all
  eight expected release and updater assets.
- The public DMG passed independent checksum, strict signature, stapled
  notarization, Gatekeeper, version, application-identity, and embedded-runtime
  verification.

Download the release from
<https://github.com/c50bossio/6fb-content-studio/releases/tag/v1.5.43>.
