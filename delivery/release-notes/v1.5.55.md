# 6FB Content Studio v1.5.55

## Reliable release verification

- Makes the isolated Electron IPC smoke wait for its Electron process to exit
  before removing its disposable profile.
- Retries only short-lived filesystem cleanup races with a fixed cap, while
  preserving non-transient cleanup failures and all functional smoke failures.
- Replaces failed, non-published tag `v1.5.54`; this release does not change
  the shipped barber workflow or Smart Trends behavior.

## Certification

This release targets **macOS arm64 only**. The tag workflow stages exactly the
DMG, updater ZIP, ZIP blockmap, and `latest-mac.yml` as a private draft and
verifies the notarized DMG before any separately approved public promotion.
Windows distribution remains deferred pending measured Mac adoption.
